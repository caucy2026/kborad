# KBoard 项目变更日志（cl.md）

> 根目录统一记录：关键过程、代码改动、验证结果、风险与待办。
> 工作目录：/Users/newlink/kemi/kboard

---

## V1.0 — 2026-07-23：fcitx5-android 移植与首版落地（V900）

### 目标
- 在 V900 双屏平板完成 fcitx5-android 的可编译、可安装、可启用。

### 关键过程
- 完成构建依赖准备：JDK/SDK/NDK/CMake/ECM/gettext 替代方案。
- 修复构建链路阻塞项：插件仓库解析、子模块缺失、代理与 TLS 问题。
- 使用 arm64-v8a debug 包部署到目标设备并切换为默认输入法。

### 关键改动
- 构建链路侧变更（仓库配置与依赖解析），确保 :app:assembleDebug 通过。
- 形成可复用部署命令（install/ime enable/ime set/启动配置）。

### 验证结果
- APK 编译成功。
- ADB 安装成功（V900）。
- 输入法启用与默认切换成功。

### 风险与待办
- 待完成双屏输入框焦点下的 IME 路由实测。
- 待完成拼音输入、候选提交与横屏 UI 适配回归。

---

## V1.1 — 2026-07-24：Gboard 基线采集与 UI 改造范围冻结

### 目标
- 建立真机 Gboard 对照基线，明确 fcitx5-android 第一阶段改造边界。

### 关键过程
- 采集主屏空闲态/拼音候选态/双屏状态。
- 识别现有 UI 控制路径（InputView、KeyboardWindow、KawaiiBar、CandidateUi）。
- 形成“先 Kotlin UI 层，后引擎层”的分阶段策略。

### 关键改动
- 输出改造清单：主题、键位重排、键帽样式、候选栏形态、工具栏尺寸。
- 冻结第一阶段范围：不改 fcitx5/libime C++ 引擎。

### 验证结果
- 基线截图与行为矩阵已建立。
- 关键风险点已明确（双屏 display 路由、候选态高度与布局约束）。

### 风险与待办
- 待做 Display 2 焦点输入场景的完整闭环验证。
- 待做 Shift/Backspace/空格滑动等交互的逐项回归。

---

## V1.2 — 2026-07-24：讯飞 ASR 接口链路沉淀与业务模式规范化

### 目标
- 固化可复用的讯飞 ASR 接入路径，支持“按住说话”和“自动聆听”两种业务模式。

### 关键过程
- 梳理 Settings.Global 参数读取 -> HTTP 鉴权 -> WebSocket 流式识别 -> 结果事件分发。
- 明确两种模式差异：cloud_vad_eos、--end-- 时机、is_finish/is_last 处理策略。

### 关键改动
- 形成接口对接文档，统一请求参数、签名规则、状态机与结束语义。
- 定义 Partial/Final/Error 三类事件抽象，降低 UI 与协议耦合。

### 验证结果
- 协议链路与字段语义已文档化，可跨项目复用。

### 风险与待办
- 待补充各异常分支的超时重试策略与监控指标。

---

## V1.3 — 2026-07-24：文档归一化（根目录集中）

### 目标
- 将项目文档统一收敛到仓库根目录，降低检索成本与协作歧义。

### 结果
- 根目录文档清单：
  - chip.md
  - fcitx5-android-port-plan.md
  - gboard-baseline-analysis.md
  - iflytek_asr_interface_doc.md
  - xtqx.md
  - cl.md（本文件）

### 约定（从本版开始生效）
- 过程变更：优先更新对应专题文档（计划/基线/接口）。
- 里程碑变更：必须回写 cl.md，包含目标、改动、验证、风险。
- 新文档默认放根目录，不再分散到子目录（代码自带 README 除外）。

---

## 下一步建议（执行序）

1. 完成 V900 双屏 IME 路由专项回归（Display 0/2）。
2. 完成 Gboard 第一阶段 UI 改造后的对照验收（截图 + 行为矩阵）。
3. 将 ASR 的异常路径（断网/鉴权失败/WS 中断）补齐自动化回归脚本。
