# KBoard 全局键盘轻量 3D 水池引擎

## 目标与边界

- 只在 `DesktopKeyboard`（全局键盘/全键盘模式）创建和运行，普通中文软键盘、数字键盘、候选栏、语音、悬浮键盘与跨屏中继不接入该引擎。
- 目标显示为 60Hz，最低验收线为持续 30Hz 以上；按键分发不等待渲染线程、鱼群模拟或音频生成。
- 保留真实 Android `KeyView` 作为触控与无障碍层。3D 水池放在按键下方，动画异常时不得影响输入。

## 63 实机硬件基线

| 项目 | 192.168.3.63 |
|---|---|
| 系统 | Android 12 / API 31 |
| CPU ABI | arm64-v8a |
| GPU | ARM Mali-G52 |
| 图形接口 | OpenGL ES 3.2 |
| D0 | 1920×1280，当前 60Hz |
| D2 | 1920×1280，固定 60Hz |
| 总内存 | 约 5.5GB |
| 检查时可用内存 | 约 2.8GB |

该配置适合 GLES 3.0 的小型常驻场景，但不适合在输入法中嵌入 Unity/Unreal 等完整游戏运行时。引擎将水池内部渲染宽度限制为 1440 像素，再由 `TextureView` 合成到键盘尺寸；字符与键帽仍由 Android 原生分辨率绘制。

## 分层架构

```text
IME 主线程
  ├─ KeyView：命中测试、按键动作、按压下沉、触觉
  ├─ 点击坐标 -> 无锁队列（最多 6 个）
  └─ 涟漪 PCM 播放（预生成，三轨轮转）

kboard-aquarium-gl 独立线程
  ├─ EGL 3.0 context + TextureView surface
  ├─ 水体 pass：渐变、流动焦散、最多 4 组涟漪
  ├─ 锦鲤 pass：程序生成鱼体网格、尾鳍摆动、花纹与高光
  ├─ 鱼群状态：自由漫游 / 投喂聚集 / 自动散开
  └─ 5 秒性能窗口：记录 FPS，并动态降低鱼数
```

按键层使用半透明深色玻璃键帽、顶部高光、边缘描线和 3dp 按压行程。鱼群与涟漪可以透过键帽看到，但不接收触控事件。

## 渲染和性能预算

60Hz 时单帧预算为 16.67ms；最低 30Hz 时单帧预算为 33.33ms。

- 水体：一个全屏四边形、单次 draw call；涟漪由片元着色器按点击位置和时间解析生成，不创建粒子 View。
- 锦鲤：默认 10 条，每条使用约 60 个三角形顶点和一次 draw call；位置、速度和避让在渲染线程更新。
- 内存：不加载视频、不保存帧缓存历史、不使用大尺寸鱼纹理；网格、着色器和涟漪数组均常驻复用。
- 热路径：每帧不排序鱼群、不查询 uniform、不分配涟漪数组；点击队列有上限，连续输入不会无限堆积动画事件。
- 动态降级：5 秒平均 FPS 低于 44 时把鱼数降到 7，低于 32 时降到 5；连续两个窗口高于 55 FPS 时逐步恢复。
- 生命周期：退出全局键盘立即停止 EGL 线程并释放 surface；普通键盘期间 GPU 场景不运行。

## 交互状态机

1. 空闲：每条鱼拥有独立目标、深度、速度与摆尾相位，缓慢游动并相互避让。
2. 按下：按键动作继续走原输入链路；同时把点击位置归一化后投递给渲染线程，只播放一次程序生成的水滴/涟漪 PCM。
3. 涟漪：最多保留最近 4 次点击，主环与次级回波扩散约 1.75 秒。
4. 聚集：最近一次点击成为 2.4 秒投喂点；鱼群带小幅个体偏移靠近，避免完全重叠。
5. 散开：聚集计时结束后，各鱼重新选择漫游目标，平滑恢复自由游动。

连续点击采用“最近位置优先”，但旧涟漪仍可完成衰减。按键释放只有视觉回弹和已配置的触觉，不播放第二声音。

## 验收

- 只使用签名 Release APK 安装到 63，不安装 Debug。
- 打开全局键盘后检查 `KBoardAquarium` 日志，每 5 秒应输出一次 `fps`、活动鱼数和内部 surface 尺寸。
- 空闲至少观察 30 秒；连续触摸不同键位，确认涟漪中心与触点一致、鱼群改变方向并聚集、按键字符立即生效。
- 退出全局键盘后确认不再输出 FPS 日志；过滤 `FATAL EXCEPTION`、EGL/GL shader 错误和 `OutOfMemoryError`。
- D0、D2 分别复测，最低持续 FPS 必须高于 30；若降级到 5 条鱼仍低于 30，则下一步降低水池内部渲染宽度，而不是阻塞或简化输入链路。
