# KBoard 全局键盘轻量 3D 水池引擎

## 目标与边界

- 只在 `DesktopKeyboard`（全局键盘/全键盘模式）创建和运行，普通中文软键盘、数字键盘、候选栏、语音、悬浮键盘与跨屏中继不接入该引擎。
- 动画固定以 30Hz 更新，单帧预算为 33.33ms；按键分发不等待渲染线程、鱼群模拟或音频生成。
- 保留真实 Android `KeyView` 作为触控与无障碍层。3D 水池放在按键下方，动画异常时不得影响输入。

## 63 实机硬件基线

| 项目 | 192.168.3.63 |
|---|---|
| 系统 | Android 12 / API 31 |
| CPU ABI | arm64-v8a |
| GPU | ARM Mali-G52 |
| 图形接口 | OpenGL ES 3.2 |
| D0 | 1920×1280，当前 60Hz |
| D2 | 1920×1280，固定 60Hz |
| 总内存 | 约 5.5GB |
| 检查时可用内存 | 约 2.8GB |

该配置适合 GLES 3.0 的小型常驻场景，但不适合在输入法中嵌入 Unity/Unreal 等完整游戏运行时。引擎将水池内部渲染宽度限制为 1080 像素，再由 `TextureView` 合成到键盘尺寸；字符与键帽仍由 Android 原生分辨率绘制。1440 内部宽度在持续交互时曾降到约 15 FPS，因此不能再作为该硬件的默认值。

## 分层架构

```text
IME 主线程
  ├─ KeyView：命中测试、按键动作、按压下沉、触觉
  ├─ DOWN / MOVE / UP -> 无锁队列（最多 12 个）
  └─ SoundPool 播放（预加载，四个真实水滴切片轮转）

kboard-aquarium-gl 独立线程
  ├─ EGL 3.0 context + TextureView surface
  ├─ 水体 pass：渐变、流动焦散、最多 4 组涟漪
  ├─ 锦鲤 pass：程序生成鱼体网格、尾鳍摆动、花纹与高光
  ├─ 鱼群状态：摆尾脉冲 / 水阻惯性 / 自由漫游 / 连续跟手 / 离手散开
  └─ 5 秒性能窗口：记录 FPS，并动态降低鱼数
```

按键层使用半透明深色景深键帽、顶部高光、边缘描线和 3dp 按压行程。鱼群与涟漪位于键帽下方，可以透过键面和键缝看到，但不接收触控事件；Android 圆形 `RippleDrawable` 已移除，触摸反馈全部由水面 Shader 生成。

## 渲染和性能预算

30Hz 时单帧预算为 33.33ms。该频率是当前 V900 全局键盘的固定目标，避免输入法常驻场景为 60Hz 动画持续占用不必要的 GPU 资源。

- 水体：一个全屏四边形、单次 draw call；涟漪由片元着色器按点击位置和时间解析生成，不创建粒子 View。
- 锦鲤：默认 10 条，每条使用约 200 个三角形顶点和一次 draw call；鱼身、连续尾幕和胸鳍有独立材质标记，划水相位、推进、水阻、转向、侧倾和避让在渲染线程更新。
- 内存：不加载视频、不保存帧缓存历史、不使用大尺寸鱼纹理；网格、着色器和涟漪数组均常驻复用。
- 热路径：每帧不排序鱼群、不查询 uniform、不分配涟漪数组；点击队列有上限，连续输入不会无限堆积动画事件。
- 动态降级：5 秒平均 FPS 低于 26 时把鱼数降到 7，低于 21 时降到 5；连续两个窗口高于 28.5 FPS 时逐步恢复。
- 生命周期：退出全局键盘立即停止 EGL 线程并释放 surface；普通键盘期间 GPU 场景不运行。

## 交互状态机

1. 空闲：每条鱼拥有独立目标、深度、前向速度、体型、三色调色板、四种花纹之一与划水相位。鱼身保持刚体，柔性行波只从尾柄传到尾膜；空闲尾摆按实际力度约为 1.6–2.8Hz，固定保留部分鱼在最下方按键和功能条覆盖区巡游，其余鱼覆盖完整键盘高度。
2. 按下：按键动作继续走原输入链路；同时把点击位置归一化后投递给渲染线程，只播放一次从四个 CC0 真实水滴切片中轮转的样本。
3. 涟漪：最多保留最近 4 次点击。片元着色器计算低振幅水面高度、坡度、轻微折射和波峰高光；波前随水流缓慢漂移，只有小幅非等比拉伸和不规则边缘，约 2.35 秒自然衰减，不显示规则亮圆环或整屏镜头扭曲。
4. 移动：只更新最近触点，不重复创建涟漪或声音；鱼群持续通过肌肉控制追踪手指，而按键命中仍由原生 `KeyView` 独立完成。底部功能条的空白水域也把触摸转发给同一状态机。
5. 聚集：手指保持接触期间，10 条鱼提高肌肉响应；尾摆随实际力度最高约 5.4Hz。可见尾膜每完成一个半周期并经过中线才结算一次推进脉冲；背对触点的鱼先用双胸鳍制动和差动转身，对准后才允许尾鳍推进。
6. 抬手：立即结束吸引，为每条鱼分配不同散开目标；约 3.4 秒后恢复各自的路线、同群跟随或双鱼玩耍，不让所有鱼同步回到一个编队。

连续点击采用“最近位置优先”，但旧涟漪仍可完成衰减。按键释放只有视觉回弹和已配置的触觉，不播放第二声音。

## 金鱼推进模型

旧实现的 CPU 每帧把速度插值到目标速度并直接更新位置，而 GPU 又按自己的 `uTime` 计算尾摆；两者只有很弱的速度调制关系，所以尾巴即使在摆，鱼体仍呈现恒速漂移。

当前每条鱼只保留一个权威划水时钟 `swimPhase`：

1. 渲染线程以固定 30Hz 推进相位。CPU 采样尾膜约 72% 长度处与 Shader 相同的延迟相位，检测尾膜是否从一侧跨过中线。
2. 只有完成的半次摆尾能增加一次前向动量；两次脉冲之间没有连续隐藏推力，鱼只依靠上一次脉冲的惯性滑行，并持续受到线性、二次水阻和胸鳍制动。
3. 世界位置按非负 `forwardSpeed` 只积分一次。路线、同伴、触点和所谓“目标速度”不能直接写入速度或坐标，快速位移只能通过单位时间内更多的可见摆尾脉冲累积。
4. 目标、避让和投喂只决定期望朝向，不直接改变位置；实际转向量受本次抓水力度限制，侧倾再由实际转向量计算。
5. 同一个 `swimPhase` 作为 `uSwimPhase` 传给 GLES 顶点着色器。鱼身保持刚体，尾膜从固定尾根开始，沿长度增加振幅并累计约 `1.34rad` 延迟；屏幕位移与可见尾鳍来自同一次相位推进，不再各自播放动画。

最终仍需把物理结果写入鱼体世界坐标才能在屏幕上移动；这里消除的是独立、恒定的模型平移动画，而不是取消渲染所必需的位置变换。动作与位移结合并不意味着把位移逐帧开关：真实水体会把离散尾摆转化为连续速度，二次门控反而会造成停顿和僵硬。

## 池塘个体代理（V1.27）

V1.27 不再让目标点直接控制位置或朝向。每帧固定经过以下单向数据流，后层不得反写前层：

```text
个人路线 / 局部同伴 / 投喂 / 边缘预测
                  ↓
           期望方向与速度
                  ↓
尾鳍力度 / 左胸鳍力度 / 右胸鳍力度 / 制动力
                  ↓
       推力 / 水阻 / 制动 / 偏航转矩
                  ↓
       前向速度 / 角速度 / 世界位置
                  ↓
        同一执行器参数驱动 GLES 网格
```

### 个体与社交

- 每条鱼有独立的不规则闭合路线、路线方向、中心、横纵半径、巡游速度和水层。路线相位按实际前进距离推进，鱼停下时路线也停止，不存在移动目标把鱼拖走。
- 鱼群分为两个松散小群。所有鱼互相避碰；只有处在前向局部视野中的同群邻居参与方向协调和聚合，避免十条鱼形成机械同步队列。
- 个体按不同起始时间在个人巡游、尾流位置跟随、双鱼绕游三个状态间轮换；每个状态保持 6.5–13 秒，以迟滞替代逐帧随机选择。
- 点击投喂临时覆盖行为目标，但仍只产生肌肉控制量；鱼必须通过加大摆尾、差动胸鳍转向和靠近后的双鳍制动抵达触点。

### 执行器与物理约束

- 尾鳍：CPU 在与 Shader 相同的柔性尾膜代表点检测半次摆尾完成事件，每次事件只增加一个前向动量脉冲；尾巴没有经过中线就不能获得本次加速。
- 左右胸鳍：共相位但具有独立力度；对称展开增加制动和水阻，单侧加大产生差动偏航转矩，不再附带不可见的前向平移推力。
- 世界速度只能由尾鳍脉冲减去线性、二次水阻和胸鳍制动后积分得到，并钳制为非负；鱼不会为了追目标倒着移动。
- 朝向只能由尾鳍抓水与左右胸鳍差动产生的角加速度积分得到。路线、群游、投喂和边缘只能提交转弯意图，不能直接设置 `heading`。
- 边缘使用随速度增长的前视距离提前请求转向和制动；越界夹取仅为数值安全网，只降低速度，不反射速度、不翻转朝向。

### GPU 动作同步

每条鱼每帧把 `swimPhase`、`finPhase`、`tailDrive`、`leftFinDrive`、`rightFinDrive`、`turnDrive`、`brakeDrive`、实际速度和角速度侧倾传给顶点 Shader。鱼身保持刚体；尾幕和左右胸鳍均来自实际执行器状态，不额外播放独立动画。连续尾幕使用 6×5 网格，尾根固定，根尖相位逐步延迟；胸鳍以固定根部为铰接点，但频率和 Z 轴幅度明显低于尾鳍，避免形成四片同步拍翼。

## 连续尾幕与 C 型快速转身（V1.29）

- 鱼身长度约占总长六成，尾幕约占四成；尾幕是一张连续网格，只在末端用顶点位置形成浅缺口，不再把上下两叶作为两个可分别变形的扇面。
- 正常游动中尾根先改变曲率，尾尖累计约 `1.34rad` 时间延迟，形成从前向后的柔性行波。尾幕只做水平方向扫水和静态小曲面，不再让两叶周期性反向翻转 Z 轴。
- 大方向误差产生 `turnDrive`。超过阈值后，尾幕沿长度卷成 C 形，最大曲率角约 `5.2rad`；卷曲时膜宽自动收拢，防止宽尾幕自交后呈现两片翅膀。
- 首次完成的尾鳍半周期同时产生偏航脉冲。投喂时角速度上限为 `8.8rad/s`；目标对准后额外角阻尼立即耗散旋转，因此表现为“卷尾—瞬转—停转”，不是直接改模型角度或持续原地旋转。
- 为消除首次转身最多等待半个低速尾摆周期的迟钝感，大角度转向的上升沿会锁存一次卷尾启动事件：尾幕同帧进入 C 形、旧方向速度降至 42%，并施加一次 `2.8 + 2.2 × tailDrive` 偏航脉冲。锁存保证该脉冲不会逐帧重复；后续转动仍由可见摆尾事件驱动。
- 胸鳍只保留 `0.7–2.25Hz` 的慢速转向/制动动作，平面和纵深幅度约为旧候选的三分之一；推进仍由可见尾鳍脉冲负责。

## 急转、冲刺与到点制动（V1.28）

点击后仍严格遵守“动作先于位移”：

1. 所有鱼收到同一投喂意图，但不直接改位置或朝向。目标速度为 `0.80–0.92 NDC/s`，投喂肌肉响应为 `9.2/s`。
2. 朝向误差大时，左右胸鳍差动和尾鳍抓水产生偏航转矩；胸鳍制动力会把尾鳍沿旧朝向的前向推力重定向为紧凑转身，避免背对目标时先滑远。
3. 鱼体对准触点后，制动下降，实际尾鳍力度同时提高摆幅、摆频和前向推力。因此只有动作明显加快时，世界坐标才会快速推进。
4. 到点前的制动半径为 `0.20 + forwardSpeed × 0.34`。速度越快越早展开双胸鳍，目标速度和尾鳍投喂增益随剩余距离下降，避免穿点后反复绕大圈。
5. 投喂期间弱化群游分离与队形项，只保留近距防碰；4.6 秒结束后再恢复个人路线、跟随和玩耍。

`.63` 最终实测中，平均触点距离从 `0.615` 降到 0.8 秒 `0.536`、1.57 秒 `0.356`、2.34 秒 `0.189`、3.11 秒 `0.115`；3.11 秒时最远个体为 `0.181`。10 条鱼保持 29.2–29.9 FPS，没有触发动态降级。

## 触水首帧、旋转与底部覆盖层（V1.30）

- 触水第一帧不能依赖 `sin(age)`，因为 `age=0` 时振幅必为零。当前首帧先绘制约 0.16–0.30 秒的非对称凹陷、偏心高光和轻微不规则接触带，再过渡到低振幅的非圆形传播波；不使用规则圆圈点击动画。
- `TextureView` 尺寸改变时必须同时调用 `SurfaceTexture.setDefaultBufferSize()` 并更新 GLES viewport。只改 viewport 会让 1080 宽内部缓冲在横竖屏往返后保留错误比例，表现为鱼体变小且不能恢复。
- 语音键和底部操作条位于水族视图上方，它们以不消费事件的观察器向引擎转发 `DOWN / MOVE / UP`；鱼群、涟漪和一次水滴声可以响应，按键与 ASR 仍由原控件处理。
- 第六行避让使用 `BaseKeyboard` 的真实底部空间参数，而不是依赖父布局动态 padding；该参数默认 0，只有全局 `DesktopKeyboard` 传入 44dp。水族 Surface 继续铺满底边，普通页面键盘布局不变。
- ASR 过程中候选/语音栏根视图保持固定尺寸，只切换内部空闲提示、实时 partial、校准和 final 内容，避免语音按下时触发 Surface 重排、跳动或缩放。

## 验收

- 只使用签名 Release APK 安装到 63，不安装 Debug。
- 打开全局键盘后检查 `KBoardAquarium` 日志，每 5 秒应输出一次 `fps`、活动鱼数和内部 surface 尺寸。
- 空闲至少观察 30 秒；连续触摸不同键位，确认涟漪中心与触点一致、鱼群改变方向并聚集、按键字符立即生效。
- 退出全局键盘后确认不再输出 FPS 日志；过滤 `FATAL EXCEPTION`、EGL/GL shader 错误和 `OutOfMemoryError`。
- D0、D2 分别复测，持续 FPS 应接近 30；若降级到 5 条鱼后仍长期低于 21，则下一步降低水池内部渲染宽度，而不是阻塞或简化输入链路。

## 2026-08-21 `.63` Release 实测

- 当前最终版本：`44fdc79e`，Android 12/API 31，Mali-G52，D0 1920×1280@60Hz；水族动画固定 30Hz。
- 首版失败原因：Mali GLSL 编译器将 `patch` 视为保留字；变量改为非保留名称后，水体与鱼群程序均成功链接。新增 shader 标识符必须避开 OpenGL ES 保留关键字，构建成功不能代替目标 GPU 验证。
- 当前默认内部 surface 宽度限制为 1080；同硬件候选版在 1080×451、10 条鱼时连续保持 29.3–29.9 FPS，未触发 7/5 条鱼降级。1440 宽在持续交互时曾跌到约 15 FPS，已经停用。
- Release 安装、便签聚焦、全局模式、底部鱼群活动范围、白色语音入口、静态截图、录屏和过滤 logcat 均完成；未发现崩溃或 EGL/GL 错误。语音恢复 160ms 按住阈值、移动取消、partial 实时字幕、松开校准态、final 预览 600ms 后单次提交，`RECORD_AUDIO`、`INTERNET`、`ACCESS_NETWORK_STATE` 权限链均保持有效。由于完整语音真机会话会向讯飞上传现场音频，未在缺少该项明确授权时远程触发。
- 当前水滴声来自 BigSoundBank `Drops of water #1` 的 4 个 CC0 录音切片，由 `SoundPool` 预加载并以 30% 系数播放；只在按下播放一次，移动与释放不播放。来源：https://bigsoundbank.com/drops-of-water-1-s1384.html ，可重复切片脚本为 `fcitx5-android/scripts/prepare-aquarium-water-touch.py`。
- 当前最终 Release 为 `e2e1813b`，APK SHA-256：`a7f37aaaf60949c8d5adc9367bc2d23fb78970d52964654ccb6b4a135452ef00`，已在 `.63` 覆盖安装。遵照用户要求没有用 ADB 启动或自动触摸最终版；连续尾幕、C 型快速转身和真实声音由用户现场验收。
